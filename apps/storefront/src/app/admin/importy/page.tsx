import { redirect } from 'next/navigation';
import { isAdminAuthenticated } from '../auth';
import { queryNeon } from '../../../lib/neon-client';
import { runSyncJob, saveSyncJobSettings } from '../actions';

export const dynamic = 'force-dynamic';

const modeLabels: Record<string, string> = { full: 'Úplný import katalógu', 'stock-price': 'Synchronizácia skladov a cien', sample: 'Testovací import', images: 'Synchronizácia obrázkov' };

export default async function AdminImports({ searchParams }: { searchParams: Promise<{ saved?: string; run?: string; error?: string }> }) {
  if (!(await isAdminAuthenticated())) redirect('/admin');
  const params = await searchParams;
  const [batches, imageStats, quality, jobs] = await Promise.all([
    queryNeon<{ id: string; mode: string; status: string; total_read: number; imported_count: number; filtered_count: number; started_at: string; completed_at: string | null }>('SELECT id, mode, status, total_read, imported_count, filtered_count, started_at, completed_at FROM sync_batches ORDER BY started_at DESC LIMIT 30'),
    queryNeon<{ total: string; with_images: string; multi_images: string; missing_images: string }>("SELECT COUNT(*)::text AS total, COUNT(*) FILTER (WHERE COALESCE(jsonb_array_length(images),0)>0)::text AS with_images, COUNT(*) FILTER (WHERE COALESCE(jsonb_array_length(images),0)>1)::text AS multi_images, COUNT(*) FILTER (WHERE COALESCE(jsonb_array_length(images),0)=0)::text AS missing_images FROM products"),
    queryNeon<{ count: string }>('SELECT COUNT(*)::text AS count FROM products WHERE quality_score IS NOT NULL AND quality_score < 60'),
    queryNeon<{ job_key: string; name: string; description: string; schedule_cron: string | null; enabled: boolean; last_requested_at: string | null }>('SELECT job_key, name, description, schedule_cron, enabled, last_requested_at FROM sync_job_settings ORDER BY job_key'),
  ]);
  const stats = imageStats[0];
  return <div><h2 className="text-2xl font-bold">Importy a automatizácie katalógu</h2><p className="mt-1 text-sm text-slate-600">Každú synchronizáciu môžete samostatne naplánovať alebo spustiť okamžite.</p>{params.saved ? <p className="mt-4 rounded-lg bg-emerald-50 p-3 text-sm text-emerald-700">Nastavenie automatizácie bolo uložené.</p> : null}{params.run ? <p className="mt-4 rounded-lg bg-emerald-50 p-3 text-sm text-emerald-700">Synchronizácia bola odoslaná na spustenie.</p> : null}{params.error ? <p className="mt-4 rounded-lg bg-rose-50 p-3 text-sm text-rose-700">Nepodarilo sa spustiť automatizáciu. Skontrolujte GITHUB_TOKEN v prostredí.</p> : null}<div className="mt-6 grid gap-4 lg:grid-cols-2">{jobs.map((job) => <div key={job.job_key} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex items-start justify-between gap-3"><div><h3 className="font-bold">{job.name}</h3><p className="mt-1 text-sm text-slate-600">{job.description}</p></div><span className={`rounded-full px-2 py-1 text-xs font-semibold ${job.enabled ? 'bg-emerald-50 text-emerald-700' : 'bg-slate-100 text-slate-500'}`}>{job.enabled ? 'Aktívna' : 'Vypnutá'}</span></div><form action={saveSyncJobSettings} className="mt-4 flex flex-wrap items-end gap-3"><input type="hidden" name="job_key" value={job.job_key} /><label className="text-xs font-semibold">Cron plán<input name="schedule_cron" defaultValue={job.schedule_cron || ''} placeholder="napr. 0 * * * *" className="mt-1 w-48 rounded-lg border p-2 text-sm" /></label><label className="flex items-center gap-2 pb-2 text-xs font-semibold"><input type="checkbox" name="enabled" value="true" defaultChecked={job.enabled} /> Povolené</label><button className="rounded-lg border border-slate-300 px-3 py-2 text-sm font-semibold">Uložiť plán</button></form><form action={runSyncJob} className="mt-3"><input type="hidden" name="job_key" value={job.job_key} /><button className="rounded-lg bg-slate-900 px-3 py-2 text-sm font-semibold text-white">Spustiť teraz</button>{job.last_requested_at ? <span className="ml-3 text-xs text-slate-500">Posledná požiadavka: {new Date(job.last_requested_at).toLocaleString('sk-SK')}</span> : null}</form></div>)}</div><div className="mt-6 grid gap-4 md:grid-cols-4">{[['Produkty', stats?.total], ['S obrázkom', stats?.with_images], ['Viac obrázkov', stats?.multi_images], ['Bez obrázka', stats?.missing_images]].map(([label, value]) => <div key={label} className="rounded-2xl border border-slate-200 bg-white p-4"><p className="text-xs uppercase text-slate-500">{label}</p><p className="mt-2 text-2xl font-bold">{value || '0'}</p></div>)}</div><p className="mt-4 rounded-xl bg-amber-50 p-4 text-sm text-amber-800">Produkty s quality score pod 60: <strong>{quality[0]?.count || '0'}</strong></p><div className="mt-6 overflow-x-auto rounded-2xl border border-slate-200 bg-white"><table className="min-w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="px-4 py-3">Dávka</th><th className="px-4 py-3">Režim</th><th className="px-4 py-3">Stav</th><th className="px-4 py-3">Načítané</th><th className="px-4 py-3">Filtrované</th><th className="px-4 py-3">Začiatok</th></tr></thead><tbody className="divide-y divide-slate-100">{batches.map((batch) => <tr key={String(batch.id)}><td className="px-4 py-3 font-mono text-xs">{String(batch.id).slice(0, 8)}</td><td className="px-4 py-3">{batch.mode}</td><td className="px-4 py-3">{batch.status}</td><td className="px-4 py-3">{batch.imported_count ?? batch.total_read ?? 0}</td><td className="px-4 py-3">{batch.filtered_count ?? 0}</td><td className="px-4 py-3 text-slate-500">{new Date(batch.started_at).toLocaleString('sk-SK')}</td></tr>)}</tbody></table></div></div>;
}
